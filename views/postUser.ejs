<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8" />
    <title>Abiturs</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css" />
    <link rel="shortcut icon" href="/css/favicon.ico" />
</head>

<body>

    <div class="content">
        <% if (typeof user === 'undefined') { user = {}; } %>

        <h3><%= titleButton %></h3>
        <!-- 
            в каждом объекте на web-форме должен быть атрибут
            "name", по которому и будет добавляться поле в объект req.body
        -->
        
        <form method="POST" action="/postUser">
            <input type="hidden" name="idUser" value="<%= user.idUser || '' %>" /> <!-- остальные поля -->
                
            <input class="value"
                type="text" 
                name="lastName" 
                minlength="1" maxlength="255" size="32" required
                placeholder="Фамилия" 
                value="<%= user.lastName || '' %>"> <br> 
        
            <input class="value"
                type="text" 
                name="firstName" 
                minlength="1" maxlength="255" size="32" required
                placeholder="Имя" 
                value="<%= user.firstName || '' %>"><br>
        
            <label>Пол:</label><br>
            <label>
                <input type="radio" name="gender" value="1" <% if (user.gender == 1) { %> checked <% } %> > муж
            </label>
            <label>
                <input type="radio" name="gender" value="0" <% if (user.gender == 0) { %> checked <% } %> > жен
            </label>
            <br><br>   
                
            <label>Группа:</label><br>
            <select name="idGroup" required>
                <% groups.forEach(group => { %>
                    <option value="<%= group.idGroup %>" <% if (user.idGroup == group.idGroup) { %> selected <% } %> >
                        <%= group.nameGroup %>
                    </option>
                <% }) %>
            </select><br><br>
            
            <button type="submit" class="green">
                <%= titleButton %>
            </button>
        </form>
        
        <a href="/getUsers" class="back-link">Вернуться к списку пользователей</a>

    </div>

</body>
</html>